name: learn-github-actions
on: [push]
jobs:
  check-node-version:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: '>=14.0.0'
      - run: node -v
