import { IAxisScale } from '@x-calendar/core';
import * as React from 'react';

interface IDayAxisProps {
  axisScale: IAxisScale;
}

export function XDayAxis(props: IDayAxisProps) {
  return <div></div>;
}
