import { IAxisScale } from '@x-calendar/core';
import * as React from 'react';

interface IYDayAxisProps {
  axisScale: IAxisScale;
}

export function YDayAxis(props: IYDayAxisProps) {
  return <div></div>;
}
